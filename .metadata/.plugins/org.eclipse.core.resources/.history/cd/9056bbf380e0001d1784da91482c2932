package comm.octest.beans;

public class QuoteFactorySingleton {
	private static final I_Quote quoteFactory = new QuoteFactory();

	public static I_Quote getInstance() {
		  if (quoteFactory == null) {
		        quoteFactory = QuoteFactorySingleton.getInstance();
		    }
		return quoteFactory;
	}
}